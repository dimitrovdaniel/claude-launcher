# claude-launcher
Claude Launcher is a cross-platform desktop app (Windows, macOS, Linux) built with Avalonia UI that lets you define per-project configurations for Claude Code sessions — including repo directory, relevant context files, and a custom start prompt - so you can jump straight into work without manual setup every time.
